{
  "name": "@slatefs/demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@slatefs/client": "workspace:*",
    "@slatefs/web-components": "workspace:*"
  },
  "devDependencies": {
    "vite": "7.0.5",
    "vitest": "3.2.4"
  }
}
